---
openapi: 3.1.0
info:
  title: authorizations_revoke
  description: ''
  version: 1.0.0
tags: []
paths:
  /ams/api/v1/authorizations/revoke:
    post:
      summary: revoke
      deprecated: false
      description: 'Use this API to get the cashier page address. After getting the
        cashier page address, you can redirect the user to the cashier page to make
        a payment.

        '
      tags: []
      parameters: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlipayAuthRevokeTokenRequest'
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlipayAuthRevokeTokenResponse'
          headers: {}
      security: []
components:
  schemas:
    Result:
      type: object
      properties:
        resultCode:
          type: string
          description: The result code.
        resultStatus:
          description: "The result status. Valid values are:\n\nS: indicates that\
            \ the result status is successful.\nF: indicates that the result status\
            \ is failed. \nU: indicates that the result status is unknown. "
          $ref: '#/components/schemas/ResultStatusType'
        resultMessage:
          type: string
          description: The result message.
      required:
      - resultCode
      - resultStatus
      - resultMessage
    ResultStatusType:
      type:
      - string
      - 'null'
      enum:
      - S
      - F
      - U
    AlipayAuthRevokeTokenRequest:
      type: object
      properties:
        accessToken:
          type: string
          description: "The access token that is used to access the corresponding\
            \ scope of the user resource. \n\nMore information:\n\nMaximum length:\
            \ 128 characters"
        extendInfo:
          type: string
      required:
      - accessToken
    AlipayAuthRevokeTokenResponse:
      type: object
      properties:
        result:
          $ref: '#/components/schemas/Result'
        extendInfo:
          type: string
      required:
      - result
    response:
      type: object
      properties:
        rrn:
          type: string
          maxLength: 32
          description: 检索参考号，可提供给用户用于与发卡行跟踪付款/退款/争议的详细信息
        arn:
          type: string
          description: 收单行参考号，收单行提供的交易号，也可提供给用户用于跟踪资金流动
        acquirerTransactionId:
          type: string
          description: 收单行交易ID
  securitySchemes: {}
servers:
- url: https://open-sea-global.alipay.com/ams/api/v1/authorizations/revoke
  description: 正式环境
security: []
